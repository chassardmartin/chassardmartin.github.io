# GitHub users and repositories displayed on a page using the `repositories`
# include. Only rendered if a page actually uses it.
github_users:
  - chassardmartin

repo_description_lines_max: 2

github_repos:
